{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Jwt": {
    "//": "DEV-ONLY signing key. Production MUST supply Jwt:Key via env var / secret store; the app refuses to start otherwise.",
    "Key": "dev-only-insecure-signing-key-change-me-please-32+chars"
  }
}
